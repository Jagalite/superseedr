# Optional systemd drop-in for strict Linux interface binding.
# Review CAP_NET_RAW's security implications before installing this file.
[Service]
AmbientCapabilities=CAP_NET_RAW
CapabilityBoundingSet=CAP_NET_RAW
NoNewPrivileges=true
